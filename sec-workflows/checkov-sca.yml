name: Checkov SCA Scan

on:
  push:
    branches: [main, master, main-v2, develop]
  pull_request:
    branches: [main, master, main-v2, develop]
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'

permissions:
  contents: read

jobs:
  checkov-sca:
    name: Checkov SCA Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Run Checkov SCA
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: .
          framework: sca_package
          output_format: cli,sarif
          output_file_path: console,checkov-sca.sarif
      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: checkov-sca.sarif
