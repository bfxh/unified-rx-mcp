# unified-rx 工具契约探针报告 — 2026-08-13

**验证对象**: unified-rx 工具集（56 core + 24 扩展 = 80 工具）
**定位**: 工具集（非智能体）——契约约束工具行为边界
**探针**: `probes/probe_01..03`（13 条契约断言）
**结果**: **13/13 verified**（2026-08-13 实测）

> 方法：探针直接调用 server 内部函数（`_call`/`_check_path`），纯本地零费用。
> 每条断言对应 spec/ 一条 MUST/SHOULD 规则。输出为数据，非指令。

## 探针结果汇总

| # | 探针 | 对应契约 | 结果 | 实测证据 |
|---|---|---|---|---|
| p01 | 沙盒越界拒绝 | §0.2 | ✅ verified | `C:\Windows\System32\notepad.exe` → `路径越界（沙盒外）` |
| p02 | bug_scan 语言门禁 | §1.1 | ✅ verified | `spec/README.md` → `Error: 仅支持 Python/Rust 文件` |
| p03 | issue_count 一致性 | §1.2 | ✅ verified | `issue_count == len(issues)`（0==0） |
| p04 | 缺陷模式检出 | §1.3 | ✅ verified | 含 undefined_var/除零/None 解引用文件 → 检出 2 项 `undefined_name` |
| p05 | std_check 多语言 | §2.1 | ✅ verified | Go 文件扫描正常 `{ok:true, issues:[]}` |
| p06 | summary.total 一致性 | §2.2 | ✅ verified | `total(0) == issues(0)` |
| p07 | 占位文字检出 | §2.3 | ✅ verified | `placeholder` 词 → `text_placeholder` 规则命中 |
| p08 | cb 索引缓存 | §3.3 | ✅ verified | 二次索引耗时 0.003s → 0.003s（未重复全量索引） |
| p09 | locate_edit candidates | §4.2 | ✅ verified | `compute_total` 命中 → candidates[1] 带 file:line=mod.py:1 |
| p10 | locate_edit 诚实空 | §4.3 | ✅ verified | 无匹配 → 返回空 candidates（不编造） |
| p11 | guard 三分级 | §5.2 | ✅ verified | 返回 verified/refuted/unverifiable 三分级结构 |
| p12 | 纯函数 parity | §6.2 | ✅ verified | `power(2,10)=1024`（无 .0）、`div(1,0)=Error` |
| p13 | rx-core 回退 | §6.3 | ✅ verified | `RX_CORE=0` → `power(3,4)=81`（Python 回退一致） |

## Findings

**无 refuted**。13 条契约断言全部与实现一致。

**过程中发现并修正的契约文档错误（1 处）**：
- spec §2.3 原写"TODO/FIXME 是占位文字 warning"——与实现不符
  （std_core `_TODO_RE` 仅统计进 `summary.todo_markers`，不判违规）。
  已修正 spec 为"TODO/FIXME 仅统计"；真实占位违规词为
  lorem/placeholder/占位/假数据/示例文案/待补充/your-name 等。

## 重放

```bash
cd unified-rx
python probes/run_all.py        # 全跑，退出码 0=全过
python probes/run_all.py --json # JSON 输出
```

## 与 session 要求的对接

- **默认挖漏洞**：p04 证明 bug_scan 本地静态检出缺陷；契约 §0.6 固化"常态自动跑"。
- **防幻觉**：p09/p10 证明 locate_edit 诚实返回（命中带证据、未命中不编造）；
  p11 证明 guard 三分级。契约 §5 固化"引用前必须验证"。
- **本地运行省 token**：p05/p08 证明 std_check/cb 本地扫描 + 缓存增量；
  契约 §0.5 固化性能（O(1) 纯函数、max_files 上限）。
